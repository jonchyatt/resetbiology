---
phase: 05-quality-assurance
plan: 02
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - app/education/page.tsx
autonomous: true
requirements:
  - QUAL-03
  - QUAL-04

must_haves:
  truths:
    - "Every research section has a practicalApplication note that links to the correct portal feature URL"
    - "Cross-domain connections are explicitly called out in every section"
    - "The general-health section studies have crossReferences connecting them to other sections"
  artifacts:
    - path: "app/education/page.tsx"
      provides: "Complete education page with all QA requirements satisfied"
      contains: "practicalApplication"
  key_links:
    - from: "each section.practicalApplication"
      to: "portal feature URL"
      via: "text containing /breath, /workout, /nutrition, /peptides, /mental-training, /vision-training, /journal, /portal, /order"
      pattern: "practicalApplication:.*/"
    - from: "general-health studies"
      to: "other research sections"
      via: "crossReferences array"
      pattern: "crossReferences:"
---

<objective>
Audit all practicalApplication fields and cross-domain connections across every research section. Fix the general-health section studies (sleep-health-1 and cold-exposure-1) which currently lack crossReferences. Verify the complete web of cross-domain connections is coherent.

Purpose: Guarantee QUAL-03 (every section links to its portal feature) and QUAL-04 (cross-domain connections explicitly called out in every section). This is the final QA task before the milestone is complete.

Output: A fully verified education page where every section has portal links and cross-domain connections, ready for production.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-quality-assurance/05-CONTEXT.md
@.planning/phases/05-quality-assurance/05-01-SUMMARY.md
@app/education/page.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Audit practicalApplication fields and fix portal URL links</name>
  <files>app/education/page.tsx</files>
  <read_first>
    - app/education/page.tsx (all 13 practicalApplication fields)
  </read_first>
  <action>
    Audit every section's practicalApplication field to confirm it mentions the correct portal feature URL.

    Expected mapping (13 sections):

    | Section ID | Expected Portal URL | Expected Feature Name |
    |---|---|---|
    | breath-training | /breath | Breath training app |
    | peptide-science | /peptides | Peptide tracker |
    | exercise-protocols | /workout | Workout tracking |
    | vision-science | /vision-training | Vision training module |
    | nback-working-memory | /mental-training | N-Back trainer |
    | ear-training | /mental-training | Pitch recognition game |
    | mental-mastery | /mental-training and /vision-training | Cognitive training tools |
    | nutrition-science | /nutrition | Nutrition tracking |
    | journaling-science | /journal | Journal feature |
    | daily-accountability | /portal | Portal daily tasks |
    | gamification-stakes | /portal | Portal gamification |
    | meditation-science | /breath | Breath training (parasympathetic overlap) |
    | general-health | (multiple) | Holistic approach |

    For each section:
    1. Confirm practicalApplication field exists (not empty)
    2. Confirm it contains the correct portal URL path (e.g., /breath, /workout)
    3. Confirm the URL is a relative path (not a full URL like resetbiology.com/...)
    4. Confirm the text naturally connects the research to the portal feature

    If any section is missing its portal URL or has the wrong URL, fix it.
    If any practicalApplication is generic/vague without a specific portal link, add the link.

    The general-health section currently says "These studies inform the holistic approach across all Reset Biology modules -- connecting breath, movement, nutrition, and recovery." This is acceptable as it references multiple modules, but it should mention at least one specific portal URL. If it does not, add a reference like "Explore these connections through our training modules at /breath, /workout, and /nutrition."
  </action>
  <verify>
    Run this verification:
    ```bash
    # Count practicalApplication fields (should be 13, one per section)
    grep -c 'practicalApplication:' app/education/page.tsx

    # Verify portal URLs are present in practicalApplication fields
    grep 'practicalApplication:' app/education/page.tsx | grep -c '/'
    ```
    Both should return 13.
  </verify>
  <acceptance_criteria>
    - grep -c 'practicalApplication:' app/education/page.tsx returns 13
    - Every practicalApplication contains at least one portal URL path (grep for '/' within practicalApplication lines returns 13)
    - breath-training mentions /breath
    - exercise-protocols mentions /workout
    - nutrition-science mentions /nutrition
    - peptide-science mentions /peptides
    - nback-working-memory mentions /mental-training
    - ear-training mentions /mental-training
    - vision-science mentions /vision-training
    - journaling-science mentions /journal
    - daily-accountability mentions /portal
    - gamification-stakes mentions /portal
    - meditation-science mentions /breath
    - general-health mentions at least one specific portal URL
    - mental-mastery mentions /mental-training
  </acceptance_criteria>
  <done>All 13 sections have practicalApplication fields with correct portal feature URLs. QUAL-03 is satisfied.</done>
</task>

<task type="auto">
  <name>Task 2: Audit cross-domain connections and fix general-health gaps</name>
  <files>app/education/page.tsx</files>
  <read_first>
    - app/education/page.tsx (all crossReferences arrays across all 13 sections)
  </read_first>
  <action>
    Audit cross-domain connections across all 13 research sections.

    **Step 1: Count crossReferences per section.**
    For each of the 13 sections, count how many studies have a crossReferences array. Build a table:

    | Section | Studies | Studies with crossRefs | Section IDs Referenced |
    |---|---|---|---|

    **Step 2: Identify sections with weak or missing cross-domain connections.**
    Every section should have at least ONE study with crossReferences pointing to a DIFFERENT domain. Same-domain references don't count for QUAL-04.

    The key cross-domain connections that MUST exist (from ROADMAP success criteria):
    - Breath-exercise-nutrition triangle (breath refs exercise, exercise refs nutrition, nutrition refs breath)
    - Cognitive chain (nback refs ear-training, mental-mastery refs exercise/nback)
    - Peptide-exercise synergies (peptide-science refs exercise-protocols)
    - Meditation-breath overlap (meditation-science refs breath-training)
    - Accountability-gamification link (daily-accountability refs gamification-stakes and vice versa)

    **Step 3: Fix general-health section.**
    The 2 general-health studies (sleep-health-1 and cold-exposure-1) currently have NO crossReferences arrays. Add them:

    For sleep-health-1 (PMID 20469800 - sleep duration meta-analysis):
    - Add crossReferences: ["breath-training", "meditation-science"]
    - Rationale: sleep quality connects to breath training (parasympathetic activation for sleep onset) and meditation (mindfulness-based stress reduction improves sleep)

    For cold-exposure-1 (PMID 10751106 - cold water immersion):
    - Add crossReferences: ["exercise-protocols", "breath-training"]
    - Rationale: cold exposure connects to exercise recovery protocols and Wim Hof breathing method

    **Step 4: Verify no section is an "island" (zero outgoing cross-references to other domains).**
    Every section must reference at least one other section through its studies' crossReferences.
  </action>
  <verify>
    Run these verification commands:
    ```bash
    # Count total studies with crossReferences (should be 58+ after adding 2 general-health)
    grep -c 'crossReferences:' app/education/page.tsx

    # Verify general-health studies now have crossReferences
    grep -A2 'id: "sleep-health-1"' app/education/page.tsx | grep crossReferences
    grep -A2 'id: "cold-exposure-1"' app/education/page.tsx | grep crossReferences

    # Verify breath-exercise connection exists
    grep -B20 'crossReferences:.*exercise-protocols' app/education/page.tsx | grep 'id: "breath-training"' | head -1 || echo "Check breath->exercise link"

    # Verify accountability-gamification bidirectional link
    grep 'crossReferences:.*gamification-stakes' app/education/page.tsx | head -1
    grep 'crossReferences:.*daily-accountability' app/education/page.tsx | head -1

    # TypeScript compiles clean
    npx tsc --noEmit 2>&1 | head -5
    ```
  </verify>
  <acceptance_criteria>
    - grep -c 'crossReferences:' returns >= 58 (was 56, +2 for general-health fixes)
    - sleep-health-1 has crossReferences including "breath-training"
    - cold-exposure-1 has crossReferences including "exercise-protocols"
    - No section is an "island" -- every section has at least one study with crossReferences to another section
    - Breath-exercise-nutrition triangle verified (bidirectional references present)
    - Meditation-breath overlap verified
    - Accountability-gamification bidirectional link verified
    - npx tsc --noEmit produces no errors
  </acceptance_criteria>
  <done>Cross-domain connections form a coherent web across all 13 sections. No section is isolated. QUAL-04 is satisfied.</done>
</task>

<task type="auto">
  <name>Task 3: Final full-page QA summary and build verification</name>
  <files>app/education/page.tsx</files>
  <read_first>
    - app/education/page.tsx (skim for overall structure completeness)
  </read_first>
  <action>
    Produce a final QA summary verifying all 4 QUAL requirements are met:

    **QUAL-01 check:** Confirm total PMID count and that all were verified in Plan 05-01.

    **QUAL-02 check:** Run a final sweep for suspicious PMID patterns:
    ```bash
    grep -oP 'pmid: "\K[0-9]+' app/education/page.tsx | while read pmid; do
      if [[ ${#pmid} -lt 5 ]] || [[ $pmid =~ ^[0-9]0000000$ ]]; then
        echo "SUSPECT: $pmid"
      fi
    done
    ```

    **QUAL-03 check:** Confirm 13 practicalApplication fields, all with portal URLs.

    **QUAL-04 check:** Confirm cross-domain connections in all 13 sections.

    **Build verification:**
    ```bash
    npx tsc --noEmit
    npm run build
    ```

    If build passes, log the final statistics:
    - Total sections: 13
    - Total studies: {count}
    - Total unique PMIDs: {count}
    - Total crossReferences entries: {count}
    - Sections with practicalApplication containing portal URLs: 13/13
    - Sections with outgoing cross-domain references: 13/13
  </action>
  <verify>
    ```bash
    # Full build must pass
    npm run build 2>&1 | tail -5

    # Final PMID count
    grep -oP 'pmid: "\K[0-9]+' app/education/page.tsx | sort -u | wc -l

    # Final crossReferences count
    grep -c 'crossReferences:' app/education/page.tsx

    # Final practicalApplication count
    grep -c 'practicalApplication:' app/education/page.tsx
    ```
  </verify>
  <acceptance_criteria>
    - npm run build exits 0 (clean build)
    - npx tsc --noEmit exits 0 (no type errors)
    - grep -c 'practicalApplication:' returns 13
    - grep -c 'crossReferences:' returns >= 58
    - grep for placeholder PMID patterns returns zero matches
    - All 4 QUAL requirements documented as satisfied in the summary
  </acceptance_criteria>
  <done>The education page passes full QA. All 4 QUAL requirements verified. Build is clean. Milestone v1.0 is complete.</done>
</task>

</tasks>

<verification>
- All 13 sections have practicalApplication with portal URLs (QUAL-03)
- All 13 sections have cross-domain connections via crossReferences (QUAL-04)
- General-health section no longer isolated (2 studies now have crossReferences)
- Build passes cleanly
- All 4 QUAL requirements satisfied
</verification>

<success_criteria>
QUAL-03: Each section has a "Practical Application" note linking to the relevant portal feature -- confirmed by auditing all 13 practicalApplication fields
QUAL-04: Cross-domain connections are explicitly called out in each section -- confirmed by verifying every section has outgoing crossReferences to other domains
Full milestone v1.0 complete: 54 requirements across 5 phases, all satisfied
</success_criteria>

<output>
After completion, create `.planning/phases/05-quality-assurance/05-02-SUMMARY.md`
</output>
