---
phase: 05-quality-assurance
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/education/page.tsx
autonomous: true
requirements:
  - QUAL-01
  - QUAL-02

must_haves:
  truths:
    - "Every PubMed link on /education resolves to a real study (no 404s, no fabricated IDs)"
    - "Zero placeholder PMIDs remain anywhere on the page"
    - "Every study object has a pmid field AND a correctly formatted link field matching that pmid"
  artifacts:
    - path: "app/education/page.tsx"
      provides: "All research studies with verified PMIDs"
      contains: "pubmed.ncbi.nlm.nih.gov"
  key_links:
    - from: "each study.pmid"
      to: "each study.link"
      via: "URL format https://pubmed.ncbi.nlm.nih.gov/{pmid}/"
      pattern: "link: \"https://pubmed.ncbi.nlm.nih.gov/"
---

<objective>
Full PMID verification sweep of every study on the /education page. Extract all ~70 PMIDs, verify each one resolves to a real PubMed page (not a 404), confirm link format consistency, and fix any issues found.

Purpose: Guarantee QUAL-01 (every study has a verified, clickable PubMed link) and QUAL-02 (zero placeholder or fabricated PMIDs remain). Phases 1-4 each verified their own PMIDs during creation, but no single full-page sweep has been done.

Output: A clean education page where every single PMID is confirmed real, or a fix applied for any that are not.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-quality-assurance/05-CONTEXT.md
@app/education/page.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract and verify all PMIDs against PubMed</name>
  <files>app/education/page.tsx</files>
  <read_first>
    - app/education/page.tsx (full file -- extract every pmid field value)
  </read_first>
  <action>
    Extract every PMID from `app/education/page.tsx` (there are ~70 studies across 13 sections).

    For EACH PMID, verify it is a real PubMed entry by fetching `https://pubmed.ncbi.nlm.nih.gov/{PMID}/` and confirming the page exists (200 response, not a 404 or search redirect).

    Use `curl -s -o /dev/null -w "%{http_code}" https://pubmed.ncbi.nlm.nih.gov/{PMID}/` for each PMID, or batch them efficiently.

    Build a verification report:
    - Total PMIDs found
    - Each PMID with its study ID and HTTP status code
    - Any PMIDs returning non-200 status (these are suspect)

    Also check for suspicious patterns:
    - Sequential round-number PMIDs (e.g., 10000000, 20000000)
    - Duplicate PMIDs used in different studies
    - PMIDs with fewer than 5 digits (likely malformed)
    - Studies missing the pmid field entirely
  </action>
  <verify>
    Run this verification command:
    ```bash
    grep -oP 'pmid: "\K[0-9]+' app/education/page.tsx | sort -u | wc -l
    ```
    This should show the total unique PMID count.

    Then verify no suspicious patterns:
    ```bash
    grep -oP 'pmid: "\K[0-9]+' app/education/page.tsx | sort | uniq -d
    ```
    This should return empty (no duplicate PMIDs).
  </verify>
  <acceptance_criteria>
    - grep -c 'pmid:' app/education/page.tsx returns the total study count (~70)
    - All unique PMIDs verified against PubMed (HTTP 200)
    - No duplicate PMIDs found (sort | uniq -d returns empty)
    - No sequential round-number PMIDs present
    - Verification report produced listing all PMIDs and their status
  </acceptance_criteria>
  <done>Every PMID on the education page has been individually verified as resolving to a real PubMed study page. Any failures have been identified and logged.</done>
</task>

<task type="auto">
  <name>Task 2: Fix any PMID or link format issues found</name>
  <files>app/education/page.tsx</files>
  <read_first>
    - Task 1 verification report (from the output of Task 1)
    - app/education/page.tsx (only the specific studies with issues, if any)
  </read_first>
  <action>
    Based on Task 1 verification results:

    IF all PMIDs verified clean (all HTTP 200, no duplicates, no format issues):
    - Log "All PMIDs verified clean -- no fixes needed"
    - Proceed to verification step

    IF any issues found:
    - For each failed PMID: search PubMed for the correct study by title/author, find the real PMID, and replace it
    - For each link format inconsistency: normalize to `https://pubmed.ncbi.nlm.nih.gov/{PMID}/` (with trailing slash)
    - For any missing link field: add it using the standard format
    - For any duplicate PMIDs: verify both studies are legitimately the same citation, or find the correct distinct PMID

    Also verify link/pmid consistency:
    - Every study with a pmid MUST have a link field
    - The link field MUST contain the same PMID: `https://pubmed.ncbi.nlm.nih.gov/{pmid}/`
    - No study should have a link but no pmid, or a pmid but no link
  </action>
  <verify>
    Run these verification commands:
    ```bash
    # Count pmid fields
    grep -c 'pmid:' app/education/page.tsx

    # Count link fields
    grep -c 'link:.*pubmed' app/education/page.tsx

    # These counts should match

    # Verify no placeholder patterns
    grep -P 'pmid: "[0-9]{8}"' app/education/page.tsx | grep -P '"(00|99|12345|00000)' || echo "No placeholders found"

    # TypeScript compiles clean
    npx tsc --noEmit 2>&1 | head -5
    ```
  </verify>
  <acceptance_criteria>
    - grep -c 'pmid:' matches grep -c 'link:.*pubmed' (every pmid has a matching link)
    - grep for placeholder patterns (00000000, 12345678, sequential round numbers) returns zero matches
    - npx tsc --noEmit produces no errors related to education page
    - If any fixes were applied, the corrected PMIDs also verify against PubMed (HTTP 200)
  </acceptance_criteria>
  <done>Every study on the education page has a verified real PMID and a correctly formatted PubMed link. QUAL-01 and QUAL-02 are satisfied.</done>
</task>

</tasks>

<verification>
- All ~70 PMIDs verified as real PubMed entries (HTTP 200)
- Zero duplicate PMIDs
- Zero placeholder/fabricated PMIDs
- Every pmid field has a matching link field in the correct format
- TypeScript compiles without errors
</verification>

<success_criteria>
QUAL-01: Every study has a verified, clickable PubMed link -- confirmed by HTTP 200 for all PMIDs
QUAL-02: Zero placeholder or fabricated PMIDs remain -- confirmed by pattern search returning zero results
</success_criteria>

<output>
After completion, create `.planning/phases/05-quality-assurance/05-01-SUMMARY.md`
</output>
