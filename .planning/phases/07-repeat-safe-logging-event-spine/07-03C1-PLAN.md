---
phase: 07-repeat-safe-logging-event-spine
plan: 03C1
type: execute
status: complete
completed_at: "2026-07-22T16:36:59-06:00"
wave: 1
depends_on:
  - 07-03B1
files_modified:
  - src/lib/workout/prismaEventStore.ts
  - app/api/workout/events/route.ts
  - tests/workout-event-route.test.ts
  - scripts/workout-event-route-proof.ts
autonomous: true
requirements:
  - LOG-02
must_haves:
  truths:
    - "Only the authenticated canonical User.id can own an accepted workout event"
    - "First acceptance and exact replay return the same five-string receipt without a second row"
    - "Ambiguous database collisions are classified from exact stored state, never error text"
    - "Production proof can delete only its exact manifest-owned event row"
  artifacts:
    - path: "app/api/workout/events/route.ts"
      provides: "Bounded authenticated event intake"
      contains: "POST"
    - path: "src/lib/workout/prismaEventStore.ts"
      provides: "Insert-only Prisma WorkoutEventStore adapter"
      contains: "findByIdentity"
    - path: "tests/workout-event-route.test.ts"
      provides: "Hostile route and adapter contract"
    - path: "scripts/workout-event-route-proof.ts"
      provides: "Commit-bound isolated production proof and exact cleanup"
---

<objective>
Connect the accepted workout event service to one authenticated, byte-bounded server route and prove its real production write, replay, conflict, recovery, and exact cleanup behavior.

Purpose: complete LOG-02 without mixing in browser synchronization or member-facing logging behavior.

Output: one clean four-file candidate, blind acceptance, matching production deployment, isolated live proof, and zero-residue cleanup receipt.
</objective>

<seven_section_ticket>

## 1. Objective

Expose `POST /api/workout/events`, meaning the authenticated single-event intake, while preserving the existing event validator, service classifier, stable receipt, and canonical member boundary.

## 2. Context and read set

Read the accepted event validator, event service, canonical-user resolver, Prisma schema/client, existing injected API route test seam, event-server tests, and the foreman ticket at `C:/Users/jonch/Projects/jarvis/data/rb-muscle-preservation-engine-v2/runtime-logs/tickets/07-03c1-authenticated-event-route.md`.

The later accepted split controls: C1 owns only the adapter, route, hostile tests, and isolated production proof. C2 owns browser drain, retry, acknowledgement persistence, and foreground synchronization.

## 3. Allowed write set

- `src/lib/workout/prismaEventStore.ts`
- `app/api/workout/events/route.ts`
- `tests/workout-event-route.test.ts`
- `scripts/workout-event-route-proof.ts`

Create data has eight caller fields. Selected and reread rows have all ten stored fields, including database-created identifier and acceptance time.

## 4. Forbidden work

No browser journal import, UI, clinical content, points, readiness, timers, awards, projections, runtime delete path, broad cleanup, new package, token/nonce/limiter system, raw driver, lock, transaction, error-message parsing, Vercel CLI, or dead `src/app/workout` edit.

## 5. Acceptance criteria

- Authentication and canonical-user resolution occur before body consumption or event-store loading.
- Direct JSON is counted to an actual 65,536-byte ceiling, fatal-decoded as UTF-8, parsed as one value, and passed to the strict existing validator.
- The closed public mapping is 401 `unauthorized`, 403 `verify_email`, 503 `identity_lookup_failed`, 400 `invalid_event`, 403 `account_mismatch`, 409 `replay_conflict`, 503 `ledger_unavailable`, and 500 `ledger_corrupt`.
- Typed structured database collisions and every ambiguous create failure reconcile through exact member/event state. Only a verified winner may become the existing checked collision sentinel.
- First acceptance, exact replay, concurrent replay, changed replay, and response-loss retry behave deterministically with no second row.
- The isolated proof durably manifests before writing, holds on preexisting identity, cleans only its exact verified row, restores baseline, and emits no secret or member identifier.

## 6. Verification commands and gates

Run the focused route suite, accepted server and spine suites, Prisma validation/generation, strict nonincremental TypeScript, full Next build, exact diff/secret/import/delete scans, clean commit, blind verifier, and Sol accept/reject. Rebase or bridge only across non-overlapping upstream changes and repeat all gates. Deploy only by pushing master, then bind the isolated proof to the exact live commit and obtain cleanup plus blind receipt PASS.

This server-only slice has no visual gate.

## 7. Return and promotion

Return base/candidate/live hashes, exact paths, all test counts, static/build results, dependency delta, blind and foreman verdicts, production proof cases, cleanup counts, Karpathy rows, deviations, and plain failures. LOG-02 receives credit only after matching live proof and zero-residue blind receipt PASS.

</seven_section_ticket>

<karpathy_rows>

**Build row**

| Hypothesis | Change | Verification | Expected result | Lesson threshold |
|---|---|---|---|---|
| The proven service can become safely reachable without building synchronization | One adapter, one bounded route, one hostile suite, one isolated proof | Offline hostile matrix, clean build, blind review, commit-bound live proof, exact cleanup | First accept and replay share one receipt and one row | Only then may C2 connect the browser journal |

**Debug row**

| Failing case | Candidate cause | Fix attempt | Post-check | Next threshold |
|---|---|---|---|---|
| A uniqueness-like failure becomes false replay or lost commit becomes false failure | Broad collision handling or no exact reread | Use typed evidence plus exact member/event state reconciliation | Winner delegates to the service; absent winner stays unavailable; conflict/corruption fail closed | Zero message parsing and zero false replay classifications |

</karpathy_rows>

<completion>

## Completion receipt

- Accepted candidate: `bddeb2495ab9741ebce79ab6b806fb0111f6e30f`.
- Live bridge: `3c5c8da8b254d14ec4fe0cbc060bd86bee89e354`, deployed by fast-forwarding `origin/master` through Git only.
- Exact scope: the four frozen files; no dependency change and no browser drain.
- Offline gates: route 20/20, inherited server 29/29, event spine 9/9, strict TypeScript PASS, Prisma validate/generate PASS, full 234-page Next production build PASS.
- Independent gates: candidate blind verifier PASS, Sol foreman ACCEPT, clean bridge verifier PASS, production receipt verifier PASS.
- Live proof: first acceptance, exact replay, concurrent replay, changed replay, and lost-response retry PASS on the exact deployed commit.
- Cleanup: exact compound identity absent, exact primary record ID absent, member event count restored to baseline, zero private proof directories, temporary relay removed, vault relocked.
- Honest warnings: local builds retained inherited missing-local-Auth0 notices and the Windows junction symlink warning; all builds exited zero and the route was present.

**Karpathy debug ratchet:** the first blind HOLD found that expected deployment identity was echoed rather than enforced and cleanup checked only the compound key. The second HOLD found the receipt-null crash window. The final repair validates the commit header on every response, persists recovered primary-ID authority before deletion, proves both lookup paths absent, and survives a lost delete response. Both hostile directions now pass independently.

**Da Vinci upgrade adopted:** cleanup authority learned after a receipt-null committed write is durably saved before deletion, which is stronger than the original same-process primary-ID reread requirement and makes cleanup-only restart provable.

</completion>
