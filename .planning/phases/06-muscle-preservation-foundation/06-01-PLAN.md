---
phase: 06-muscle-preservation-foundation
plan: 01
type: control
wave: 0
depends_on: []
files_modified:
  - .planning/PROJECT.md
  - .planning/MILESTONES.md
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
  - .planning/STATE.md
  - .planning/research/STACK.md
  - .planning/research/FEATURES.md
  - .planning/research/ARCHITECTURE.md
  - .planning/research/PITFALLS.md
  - .planning/research/SUMMARY.md
  - .planning/scripts/verify-v2-roadmap.ps1
  - .planning/phases/06-muscle-preservation-foundation/06-CONTEXT.md
  - .planning/phases/06-muscle-preservation-foundation/06-01-PLAN.md
  - .planning/phases/06-muscle-preservation-foundation/06-VERIFICATION.md
autonomous: true
requirements: []
control_only: true
must_haves:
  truths:
    - "The complete GODMODE v2 scope and accepted upgrades survive compaction in committed planning artifacts"
    - "The old candidate is preserved without becoming a release candidate"
    - "The fresh v2 worktree contains current origin/master plus planning-only commits and no runtime edits"
    - "All 105 requirements map to exactly one primary roadmap phase with explicit verification"
    - "Unresolved ownership is recorded as a hold, not silently crossed"
  artifacts:
    - path: ".planning/REQUIREMENTS.md"
      provides: "Atomic milestone contract"
      contains: "FND-01"
    - path: ".planning/ROADMAP.md"
      provides: "Dependency-ordered Phases 6 through 13"
      contains: "Phase 13"
    - path: ".planning/research/SUMMARY.md"
      provides: "Research synthesis and evidence corrections"
      contains: "Evidence Ladder"
---

<objective>
Establish the v2 control plane, preserve the superseded candidate safely, convert the entire master brief and research upgrades into atomic requirements, and create a dependency-safe GSD roadmap before runtime implementation.

Purpose: prevent scope loss, concurrent write collisions, unverifiable production changes, and attractive but unsafe implementation shortcuts.

Output: a clean, committed planning scaffold and an explicit runtime hold until ownership boundaries are resolved.
</objective>

<seven_section_ticket>

## 1. Objective

Freeze the old candidate as non-release evidence, establish the clean v2 worktree and control rail, research the state of the art, and map the complete build into GSD Phases 6 through 13.

## 2. Context and Read Set

Read the full master build, repository doctrine, mandatory Reset Biology and interface specialist findings, current remote source, old candidate diff, active ownership locks, production reconnaissance, and all five v2 research documents.

The initial source baseline was `afe437ac9ea7cd9c072cc43a130d9471a38a47fa`. During scaffolding, `origin/master` advanced to `3b63de84b76ecbf11862fb3d2db8dfe2db5399e0` through nutrition-only files; the v2 planning commits must be rebased onto it before this ticket closes. The old preserved local candidate is `c9ecf1866484825b44b96090e6560d34953df5cb` and is explicitly superseded. The original GSD milestone-start commit before rebase is `fee3f390`.

## 3. Allowed Write Set

Old workout-polish worktree:

- `.planning/phases/06-workout-polish/06-VERIFICATION.md`;
- `src/lib/workoutProtocolService.ts`;
- `tests/workout-protocol-service.test.ts`;
- one local preservation commit containing only those three already-present paths;
- no content edits and no push.

Jarvis control plane:

- `data/rb-muscle-preservation-engine-v2/ACTIVE-WORK-LOCK-gmpv2sol.md`;
- `data/codex-work-packages/rb-muscle-preservation-engine-v2/MASTER-BUILD.md`;
- `data/codex-work-packages/rb-muscle-preservation-engine-v2/FOREMAN.md`;
- `data/codex-work-packages/rb-muscle-preservation-engine-v2/LOCK.json`;
- `data/codex-work-packages/rb-muscle-preservation-engine-v2/EVIDENCE.md`;
- `data/rb-workout-polish/ACTIVE-WORK-LOCK-c8c37277.md`;
- `data/foreman-adoption/LEDGER-INDEX.md`.

Fresh product worktree:

- the exact planning and research files listed in this plan’s `files_modified` block;
- no runtime source.

External state:

- read-only source and live reconnaissance;
- lock-registry and owner-coordination messages only;
- no member, database, browser, Atlas, Vercel, seed, or production write.

## 4. Forbidden Work

- Do not reset, merge, cherry-pick, push, or release the superseded old candidate.
- Do not edit runtime source, protocol seeds, clinical content, the nutrition scorecard, credentials, or production environment settings.
- Do not assign a protocol, submit readiness, log a session, award points, or clean data not created by this run.
- Do not commandeer an owned browser session.
- Do not print or reproduce any credential.
- Do not delete completed v1 GSD phase history.

## 5. Acceptance Criteria

- The old worktree is clean at one local preservation commit and that commit is not on `origin/master`.
- The v2 worktree contains current `origin/master` and only committed planning/research changes above it.
- Research covers product features, architecture, stack, failure modes, and evidence honesty.
- Requirements preserve every item in the master brief and every accepted upgrade.
- Roadmap phases are dependency ordered, use Phases 6 through 13, and map every requirement in both directions.
- The Phase 6 context, plan, and verification contract exist.
- This control plan claims no FND or OPS requirement completion; Phase 6 runtime planning remains a separate held activity.
- Current holds name the blocked surface and the authority required to release it.

## 6. Verification Commands

Run from the fresh product worktree:

```powershell
git status --short
git merge-base --is-ancestor origin/master HEAD
powershell -NoProfile -ExecutionPolicy Bypass -File .planning/scripts/verify-v2-roadmap.ps1
git diff origin/master..HEAD --name-only
```

Inspect every changed path and reject any runtime, seed, credential, or unrelated file.

## 7. Return Format

Return the base and planning commit hashes, files changed, total requirements and coverage, Phase 6 through 13 map, research upgrades, explicit holds, deviations, and a plain-language GO/HOLD verdict. State failures plainly.

</seven_section_ticket>

<tasks>

<task type="auto">
  <name>Task 1: Preserve the superseded candidate without promoting it</name>
  <files>C:/Users/jonch/rbw-workout-polish/.planning/phases/06-workout-polish/06-VERIFICATION.md, C:/Users/jonch/rbw-workout-polish/src/lib/workoutProtocolService.ts, C:/Users/jonch/rbw-workout-polish/tests/workout-protocol-service.test.ts</files>
  <read_first>
    - The exact dirty diff for the three named paths
    - `data/rb-workout-polish/ACTIVE-WORK-LOCK-c8c37277.md`
  </read_first>
  <action>
    Confirm there are no other changed or untracked paths and scan only the named diff for secret-shaped content. If and only if both checks pass, create one local commit explicitly marked superseded and do not push, merge, cherry-pick, or release it. Update only the exact old lock and foreman ledger paths named in the allowed write set.
  </action>
  <verify>
    In the old worktree, `git status --short` is empty; `git show --name-only --pretty=format: c9ecf1866484825b44b96090e6560d34953df5cb` returns exactly the three named paths; and `git branch -r --contains c9ecf1866484825b44b96090e6560d34953df5cb` does not include `origin/master`.
  </verify>
  <done>
    The old work is recoverable locally, clean, explicitly superseded, and absent from the production branch.
  </done>
</task>

<task type="auto">
  <name>Task 2: Establish and current-base the v2 GSD scaffold</name>
  <files>.planning/PROJECT.md, .planning/MILESTONES.md, .planning/REQUIREMENTS.md, .planning/ROADMAP.md, .planning/STATE.md, .planning/research/STACK.md, .planning/research/FEATURES.md, .planning/research/ARCHITECTURE.md, .planning/research/PITFALLS.md, .planning/research/SUMMARY.md, .planning/scripts/verify-v2-roadmap.ps1, .planning/phases/06-muscle-preservation-foundation/06-CONTEXT.md, .planning/phases/06-muscle-preservation-foundation/06-01-PLAN.md, .planning/phases/06-muscle-preservation-foundation/06-VERIFICATION.md</files>
  <read_first>
    - `data/codex-work-packages/rb-muscle-preservation-engine-v2/MASTER-BUILD.md`
    - `.planning/PROJECT.md`
    - `.planning/research/SUMMARY.md`
  </read_first>
  <action>
    Preserve v1 history, create the v2 milestone, research the product and evidence, define 105 atomic requirements, create Phases 6 through 13, and retain the seven-section and blind-verification harness. Commit planning only, fetch current `origin/master`, inspect every intervening path, and rebase the planning branch only when the upstream change is non-overlapping. Stop on any runtime overlap or conflict.
  </action>
  <verify>
    `git merge-base --is-ancestor origin/master HEAD` exits zero; `git status --short` is empty after the final planning commit; and `git diff origin/master..HEAD --name-only` contains only the exact fresh-product planning paths named in this task.
  </verify>
  <done>
    The committed v2 scaffold descends from current upstream and contains no product runtime change.
  </done>
</task>

<task type="auto">
  <name>Task 3: Prove coverage, scope, and hold state independently</name>
  <files>.planning/scripts/verify-v2-roadmap.ps1, .planning/REQUIREMENTS.md, .planning/ROADMAP.md, .planning/STATE.md, .planning/phases/06-muscle-preservation-foundation/06-VERIFICATION.md</files>
  <read_first>
    - `.planning/REQUIREMENTS.md`
    - `.planning/ROADMAP.md`
    - `.planning/STATE.md`
    - `.planning/phases/06-muscle-preservation-foundation/06-VERIFICATION.md`
  </read_first>
  <action>
    Run the committed coverage verifier, inspect the complete diff, and obtain an independent plan-check verdict. Confirm that exactly 105 unique requirements appear in definitions, roadmap ownership, and traceability; the three sets are identical; each ID has one owner; phase ownership agrees; and Phase 10 explicitly depends on Phase 9. Confirm runtime remains held and this plan claims no Phase 6 product requirement completion.
  </action>
  <verify>
    `.planning/scripts/verify-v2-roadmap.ps1` exits zero with 105 definitions, 105 roadmap assignments, 105 traceability rows, and zero duplicates, omissions, or phase mismatches. `git diff --check` exits zero. The independent plan checker returns PASS or a new revision is required.
  </verify>
  <done>
    Scope is mechanically complete, write paths are planning-only and exact, and no runtime work is authorized by the control-plane pass.
  </done>
</task>

</tasks>

<karpathy_rows>

**Build row**

| Hypothesis | Change | Verification | Result | Lesson |
|---|---|---|---|---|
| A clean control scaffold will preserve the full vision and prevent an old partial candidate from becoming accidental production truth | Preserve old work locally, create a fresh v2 worktree, research the domain, write atomic requirements, and map dependencies | Clean-tree inspection, path audit, requirement count, two-way roadmap coverage | PASS at `e37d808f`: 105 definitions, 105 roadmap assignments, 105 traceability rows, zero mismatch, clean current-base tree, blind plan approval | Planning is an executable safety boundary only when every requirement and owner is visible |

**Debug row**

| Failing case | Candidate cause | Fix attempt | Post-check | Next threshold |
|---|---|---|---|---|
| Stock milestone reset would erase completed v1 phase history | Generic GSD cleanup assumes a disposable planning directory | Continue numbering at Phase 6 and record the deviation | v1 phase directories remain intact, v2 starts at Phase 6, and the blind checker passed the control scaffold | No runtime work until the separate ownership holds release and FND runtime plans pass |

</karpathy_rows>
