# Vocal Trainer III Worklist

## Completed

- [x] Add a source-backed score health artifact for Lida Rose Lead.
  - Generated by `node scripts/omr/build-lida-score-health.mjs`.
  - Checks six-flat key signature, 113 source pitches, whole notes at measures 5, 9, and 13, no stray courtesy clefs, and sync/reconciled note counts.
  - Page 197 measure 13 is source-image corrected: Audiveris compressed the printed Lead whole note after bar 12 into two half notes (`Bb3`, `Gb3`). The generated score now emits one `Bb3` whole note.
- [x] Surface score health in Vocal Trainer III.
  - The trainer reads `/musicxml/lida-rose-lead-score-health.json`.
  - The score header shows pass/fail, key, note count, and whole-note count.
- [x] Re-anchor live practice target to the corrected score lane.
  - The lock meter and coach stats use the current reconciled score note first.
  - BasicPitch extraction remains visible for comparison/editing.
- [x] Add score-derived phrase loop buttons.
  - Phrase buttons set A/B loop points and seek to the phrase start.
  - Manual A/B controls remain available.
- [x] Add lyric-aware phrase labels.
  - VT3 reads `/musicxml/lida-rose-lead-phrases.json`.
  - The manifest is manual/source-backed from `public/score/page-196.jpg`, `page-197.jpg`, and `page-198.jpg` because the current Audiveris exports do not include lyric nodes.
- [x] Add a compact coach strip.
  - Displays current score target, score status, take accuracy, average drift, and current drift.
- [x] Store per-take coach summaries.
  - Completed takes are stored in `localStorage` under `vt3_take_summaries_v1`.
  - The trainer shows recent takes with accuracy, drift, and duration, plus a clear action.
- [x] Expand score health to all available Lida Rose source parts.
  - `public/musicxml/lida-rose-score-health.json` verifies Tenor, Lead, Baritone, and Bass source pages.
  - Lead remains the only generated/rendered MusicXML part in VT3; the other three parts are source-health checked and marked `not-yet-generated`.
- [x] Add render/source review pack generation.
  - `node scripts/omr/verify-vt3-render-source.mjs` verifies source page images and the rendered OSMD score are nonblank.
  - It writes `vt3-rendered-engraving.png`, `vt3-render-source-contact.png`, and a manifest under `C:/Users/jonch/Projects/jarvis/data/vocal-trainer/runtime-logs/render-source`.
- [x] Add one-click report-bad-engraving capture.
  - The score header has a `Report engraving` action.
  - The report stores page, measure, pitch, lyric phrase, playback time, active score telemetry, viewport, and a capped SVG snapshot in `localStorage` under `vt3_engraving_reports_v1`.
  - `node scripts/omr/verify-vt3-report-capture.mjs` verifies the workflow.

## Process Refinement From The Bar-13 Miss

- The previous verification compared generated MusicXML against `omrTargets.ts`, but `omrTargets.ts` was generated from the same compressed OMR source. That made the bug self-validating.
- The source-image correction now lives in `scripts/omr/lida-lead-source-corrections.mjs` with explicit `part` and `divisions` options, so Lead-only corrections do not bleed into all-part health.
- `scripts/omr/verify-lida-lead-engraving.mjs` now independently expects `5:Cb4 9:Cb4 13:Bb3` as whole-note survival evidence.

## Latest Local Verification

- `node scripts/omr/verify-lida-lead-engraving.mjs`: PASS (`pitched notes: 113`, `whole notes: 5:Cb4 9:Cb4 13:Bb3`).
- `node scripts/omr/build-lida-score-health.mjs`: PASS.
- `cmd /c npx tsc --noEmit`: PASS.
- `cmd /c npm run build`: PASS with existing local Auth0 environment warnings.
- `VT3_URL=http://localhost:3015/pitch-defender/vocal-trainer-3 VT3_ALLOW_EMPTY_LIBRARY=1 node scripts/omr/verify-vt3-production-smoke.mjs`: PASS.
- `VT3_URL=http://localhost:3015/pitch-defender/vocal-trainer-3 node scripts/omr/verify-vt3-render-source.mjs`: PASS.
- `VT3_URL=http://localhost:3015/pitch-defender/vocal-trainer-3 node scripts/omr/verify-vt3-report-capture.mjs`: PASS.

## Remaining System Upgrades

- [ ] Generate/render Tenor, Baritone, and Bass MusicXML from the same source-part health pipeline.
- [ ] Replace the manual phrase manifest if/when the OMR export includes reliable lyric syllables.
- [ ] Add pixel/measure-level source crop alignment after source-system bounding boxes are available.
