{
  "installCommand": "rm -rf node_modules && npm install --legacy-peer-deps && ls -la node_modules/@react-three/ && ls -la node_modules/three/",
  "crons": [
    {
      "path": "/api/notifications/send",
      "schedule": "*/5 * * * *"
    },
    {
      "path": "/api/notifications/replenish-queue",
      "schedule": "0 2 * * *"
    }
  ]
}
